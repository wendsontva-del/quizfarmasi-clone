// Direção visual: o App mantém uma única experiência imersiva, sem navegação extra, como na referência do quiz.
import Home from "@/pages/Home";

export default function App() {
  return <Home />;
}
